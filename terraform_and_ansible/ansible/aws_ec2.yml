plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  tag:Project: todolist  

keyed_groups:
  - key: tags
    prefix: tag
  - key: tags.Project
    prefix: project
compose:
  ansible_host: public_ip_address